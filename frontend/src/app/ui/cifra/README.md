# `ui/cifra/` — Una cifra medida

Siempre en la voz de la máquina (mono, cifras tabulares) para que se distinga de
la voz editorial del texto.

El número que llega es el que midió el backend; la animación sólo lo trae hasta
ahí. Con `prefers-reduced-motion` aparece directamente en su valor final.
