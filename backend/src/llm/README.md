# `llm/` — OpenRouter

Generación de respuestas, en bloque o token a token. También la lectura de
imágenes que usa el motor de OCR `vision`, porque es la misma API.

Si la clave está vacía el módulo arranca igual y avisa: todo el recorrido
funciona salvo la última llamada.
